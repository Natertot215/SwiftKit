import SwiftUI

struct ContentView: View {
    var body: some View {
        Text("SwiftKit")
            .font(.title2)
            .foregroundStyle(.secondary)
            .frame(maxWidth: .infinity, maxHeight: .infinity)
    }
}
