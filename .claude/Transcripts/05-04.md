# Session — 2026-05-04

**Outcome:** SwiftKit's `.claude/` migrated to the new StudioMD four-folder convention. Pushed to `origin/main`.

## What happened

**Studio's top-level CLAUDE.md is being updated** to a new project structure. Old four-file convention (`framework.md`, `feedback.md`, `memory.md`, `lessons.md`) replaced by:

- Root: `claude.md`, `handoff.md`, `history.md`, `framework.md`
- Subfolders: `Planning/`, `Transcripts/`, `Guidelines/`

**Migration plan written and approved.** Saved at `~/.claude/plans/please-review-the-current-prancy-dahl.md`. After Nathan's corrections, the plan locked in:
- Triages → `Planning/` (not `Transcripts/` — they're work plans, not session summaries).
- `lessons.md` split into 3 domain-scoped files under `Guidelines/`, not kept monolithic.
- `Transcripts/` created empty — populated only when Nathan explicitly requests it.
- `claude.md` rewritten brief + directional, non-negotiables upfront, detail deferred to `Guidelines/`.

**Migration executed (commit `b25d725`).** 20 files changed, 539 deletions, 390 insertions:
- `CLAUDE.md` → `claude.md` (case-only rename via `git mv` because macOS filesystem is case-insensitive).
- `memory.md` → `history.md` (reframed from "current state" entries to dated chronological log).
- `handoff.md` rewritten as session-continuity doc (stripped first-time-reader narrative).
- `feedback.md`, `macuix-rules.md` → `Guidelines/` (verbatim move).
- `lessons.md` split:
  - `Guidelines/visual-rules.md` — L-001 (semantic primitives), L-012 (semantic tokens over literals).
  - `Guidelines/api-and-layout.md` — L-002 (verify APIs), L-003 (NavigationSplitView), L-008 (windowResizeAnchor), L-009 (sidebar width).
  - `Guidelines/build-and-process.md` — L-005 (DerivedData), L-006 (surface constraints), L-007 (modelContainer), L-010 (file placement), L-011 (large literals).
- `sidebar-plan.md`, `describe-bundles-2026-05-03.md`, all 6 `triage-*.md` → `Planning/`.
- `framework.md` ref updates (subfolder paths for `feedback.md` and `sidebar-plan.md`).

**Git remote URL added to `claude.md`** (commit `325c4c2`): <https://github.com/Natertot215/SwiftKit.git>.

**Pushed to `origin/main`.** Remote was 1 commit ahead (README update); rebased local 15-commit stack on top, then pushed cleanly. Final remote tip: `c26be61`.

**Transcripts/ populated this session** (commit pending) with summaries for 2026-05-01 through 2026-05-04.

## Decisions locked
- New `.claude/` convention is the source of truth going forward — read `claude.md` first on session start.
- Transcripts are written on explicit request, not automatically at session end.
- Case-only renames on macOS require `git mv` (filesystem is case-insensitive by default).

## Pitfalls observed
- `Write` and `rm` resolve `claude.md` and `CLAUDE.md` to the same path on case-insensitive macOS — only `git mv` properly tracks the case change. Caught this when initial `Write` to `claude.md` failed with "File has not been read yet" because it was actually the existing `CLAUDE.md`.
